import React from 'react';

class Controls extends React.Component {
    render() {
        return (
            <div>
                Min Input / Max Input / Button to create
                // two values
                // one button/range selector and create array of that range
            </div>
        );
    }
}

export default Controls;